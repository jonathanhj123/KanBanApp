"""Pure reorder logic — YOUR CODE, Task 5 (your first TDD red/green cycle).

Write tests/test_ordering.py FIRST, watch it fail, then implement this.
"""


def reorder(ids, moved_id, target_index):
    """Return a NEW list: moved_id removed (if present) then inserted at
    target_index, clamped to the valid range. The returned list's indexes
    ARE the new position values (db.move_card enumerates it).

    No database, no side effects — that's what makes it trivially testable.
    """
    raise NotImplementedError  # TODO(Task 5.3)
